import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // src/lib/permissions.ts deliberately keeps an unused `role` param on
      // permissions that are role-independent (e.g. viewBoard: (_role) =>
      // true), so every entry in the PERMISSIONS object has the same
      // `(role: UserRole) => boolean` shape callers can rely on. Recognize
      // the existing underscore-prefix convention instead of flagging it.
      "@typescript-eslint/no-unused-vars": [
        "warn",
        { argsIgnorePattern: "^_", varsIgnorePattern: "^_" },
      ],
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
